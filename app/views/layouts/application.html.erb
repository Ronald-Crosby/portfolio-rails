<!DOCTYPE html>
<html>
	<head>
		<title>Olly Robinson | Front-end Developer</title>
		<%= csrf_meta_tags %>
		<%= csp_meta_tag %>
		<%= favicon_link_tag 'favicon-olly-robinson.ico' %>

		<%= stylesheet_pack_tag 'stylesheets', 'data-turbolinks-track': 'reload' %>
		<%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
	</head>

	<body class="antialiased bg-grey relative px-4">
		<%= render 'layouts/header'%>
		<%= yield %>
		<%= render 'layouts/footer'%>
	</body>
</html>
